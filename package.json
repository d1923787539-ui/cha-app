{
  "name": "cha-app",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter frontend build",
    "start": "cd backend && node src/index.js",
    "dev": "pnpm --parallel -r dev"
  }
}

